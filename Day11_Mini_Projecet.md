# [SeSAC 도봉] 11일차 : 도메인 주도 설계와 비즈니스 로직의 캡슐화

그동안 파편적으로 학습해온 웹 프레임워크의 다양한 요소들을 결합하여, 스터디룸 예약 시스템이라는 하나의 온전한 도메인을 설계하고 구현했습니다. 단순한 기능의 나열이나 데이터의 입출력을 넘어, 예약의 충돌을 방지하고 권한을 검증하는 등 실제 비즈니스 요구사항을 코드로 번역하는 과정에 깊이 집중했습니다. 현실의 복잡한 규칙들을 데이터베이스의 스키마 제약조건과 애플리케이션의 서비스 로직으로 적절히 분배하며, 견고하고 확장 가능한 백엔드 시스템의 구조에 대해 치열하게 고민한 하루였습니다.

---

## 1. 현실의 제약을 데이터베이스 스키마로 번역하다

예약 시스템의 가장 핵심적인 비즈니스 규칙은 동일한 공간과 시간에 대한 중복 예약을 방지하는 것입니다. 이를 애플리케이션 레벨의 조건문으로만 처리할 경우, 다수의 사용자가 정확히 같은 시점에 요청을 보낼 때 동시성 이슈로 인해 데이터의 무결성이 깨질 위험이 있음을 확인했습니다.

### 복합 유니크 제약조건을 통한 원천 차단
이러한 문제를 구조적으로 해결하기 위해 데이터베이스 테이블 레벨에서 엄격한 제약조건을 설정했습니다. 예약 모델을 설계할 때 스터디룸 식별자와 예약 날짜, 그리고 시작 시간을 하나로 묶어 복합 유니크 제약조건을 부여했습니다. 이는 애플리케이션 로직에 예기치 않은 결함이나 지연이 발생하더라도, 데이터베이스가 최후의 보루로서 중복 데이터 생성을 막아준다는 점에서 시스템의 안정성을 획기적으로 높이는 설계 방식임을 깨달았습니다.

### 일대일 관계의 명확한 정의와 방어적 로직
리뷰 도메인을 구현하는 과정에서는 예약과 리뷰 사이의 관계를 일대일로 맵핑했습니다. 하나의 예약에 대해 여러 번 리뷰를 작성할 수 없다는 규칙을 시스템에 반영하기 위함입니다. 이를 위해 리뷰 모델의 외래 키에 유니크 속성을 부여하여 스키마 차원에서 중복 생성을 막아냈습니다. 더불어 서비스 계층에서도 예약 데이터의 존재 여부를 먼저 확인하고, 해당 예약의 주인이 현재 요청을 보낸 사용자와 일치하는지 대조하며, 이미 작성된 리뷰가 없는지까지 삼중으로 검증하는 방어적 프로그래밍을 수행했습니다.

---

## 2. 오늘의 집중 학습 : 계층형 아키텍처와 성능 최적화의 실전 적용

프로젝트의 뼈대가 갖춰지고 기능이 늘어날수록 라우터, 서비스, 레포지토리로 이어지는 계층형 아키텍처의 진가를 명확히 체감할 수 있었습니다. 각 계층이 타 계층의 내부 구현을 모른 채 자신의 책임에만 집중할 때, 코드는 비로소 생명력을 얻고 유지보수가 용이해짐을 절감했습니다.

### 의존성 주입과 책임의 완벽한 분리
예약 및 리뷰 생성 API를 구현하며, 라우터는 단지 클라이언트의 요청을 규격에 맞게 수신하고 결과를 반환하는 통신 중계소 역할만 수행하도록 로직을 격리했습니다. 보안을 위한 토큰 검증과 사용자 식별 절차는 의존성 주입 함수로 분리하여 라우터의 매개변수로 주입받도록 처리했습니다. 복잡한 도메인 규칙들은 모두 서비스 계층으로 이관하였으며, 서비스 계층은 데이터의 상태를 확인한 후 통과된 데이터만을 레포지토리로 넘겨 영속화를 위임했습니다.

### 지연 로딩의 함정과 조인 로드를 통한 최적화
이전에 개념적으로만 학습했던 조인 연산의 성능 최적화 기법을 오늘 구현한 예약 목록 조회 기능에 직접 적용하며 그 위력을 확인했습니다. 특정 사용자의 예약 목록을 불러올 때, 화면에 스터디룸의 이름이나 세부 정보를 함께 표시해야 하는 요구사항이 있었습니다. 이때 기본 설정인 지연 로딩을 방치하면 예약 건수만큼 스터디룸 테이블을 조회하는 추가 쿼리가 발생하여 심각한 병목을 초래합니다. 

이를 해결하기 위해 레포지토리 계층에서 데이터베이스 쿼리를 작성할 때 조인 로드 옵션을 명시했습니다. 이 단 한 줄의 설정을 통해 예약 정보와 연관된 스터디룸 정보를 데이터베이스 단에서 하나의 쿼리로 결합하여 가져오게 함으로써, 프레임워크가 주는 편리함 이면에 숨겨진 성능 저하의 함정을 우회하는 최적화 로직을 내재화할 수 있었습니다.

```python
# 서비스 계층의 방어적 검증과 레포지토리 계층의 조인 로드 적용

class ReviewService:
    def create_review(self, db: Session, review_req: ReviewCreate, user_id: int):
        # 1. 예약의 실제 존재 여부 확인
        reservation = (
            db.query(Reservation)
            .filter(Reservation.id == review_req.reservation_id)
            .first()
        )

        if not reservation:
            raise HTTPException(status_code=404, detail="예약 정보를 찾을 수 없습니다.")

        # 2. 타인의 예약에 리뷰를 남기는 행위 원천 차단
        if reservation.user_id != user_id:
            raise HTTPException(
                status_code=403, detail="본인의 예약에만 리뷰를 작성할 수 있습니다."
            )

        # 3. 비즈니스 룰 통과 시 레포지토리에 저장 위임
        return self.repository.create_review(db, review_req, user_id)


class ReservationRepository:
    def get_reservations_by_user(self, db: Session, user_id: int):
        # 예약 목록을 조회할 때 연관된 스터디룸 정보를 하나의 쿼리로 Eager Loading 처리
        return (
            db.query(Reservation)
            .options(joinedload(Reservation.study_room))
            .filter(Reservation.user_id == user_id)
            .order_by(Reservation.date.desc())
            .all()
        )