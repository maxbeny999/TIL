# [SeSAC 도봉] 17일차 : 파이썬과 자바스크립트의 논리적 대조

오늘부터는 웹 브라우저의 동적인 제어를 담당하는 자바스크립트 생태계로 진입했습니다. 새로운 언어의 문법을 무작정 암기하는 대신, 그동안 뼈대를 다져온 파이썬과의 철학적 차이를 대조하며 학습을 진행했습니다. 변수의 할당부터 데이터 구조를 순회하는 방식까지, 두 언어가 동일한 목적을 달성하기 위해 어떠한 각기 다른 설계 방식을 채택했는지 그 미묘한 차이를 파악하고 시스템적 사고를 확장하는 시간이었습니다.

---

## 1. 상태의 통제와 타입의 엄격성

가장 먼저 체감한 변화는 데이터를 담는 그릇인 변수를 선언하고 통제하는 방식이었습니다. 파이썬에서는 별도의 선언 키워드 없이 변수를 생성했지만, 자바스크립트에서는 상태의 가변성에 따라 상수를 의미하는 const와 변수를 의미하는 let을 명확히 구분하여 사용해야 함을 확인했습니다. 이는 개발자의 실수로 인한 예기치 않은 상태 변경을 원천적으로 차단하는 방어적 메커니즘으로 작용합니다.

### 존재하지 않는 상태의 세분화
파이썬의 None에 대응하는 개념이 자바스크립트에서는 의도적으로 값을 비워둔 상태인 null과, 선언만 되고 값이 할당되지 않은 미지의 상태인 undefined로 나뉘어 있음을 배웠습니다. 에러를 추적할 때 이 두 가지 상태의 차이를 인지하는 것이 버그의 근원지를 찾는 중요한 단서가 됨을 깨달았습니다.

### 비교 연산의 엄격함
또한 자바스크립트 고유의 느슨한 타입 변환으로 인해 발생하는 논리적 오류를 방지하기 위해, 값과 데이터 타입을 동시에 비교하는 일치 연산자(===)의 사용이 필수적임을 실습했습니다. 숫자 1과 문자 1을 동등 연산자(==)로 비교하면 참이 반환되지만, 일치 연산자를 사용하면 거짓이 반환되는 현상을 코드로 직접 확인하며 데이터 타입에 대한 엄격한 통제의 필요성을 절감했습니다.

---

## 2. 오늘의 집중 학습 : 함수형 사고와 데이터 파이프라인

오후에는 자바스크립트의 함수 선언 방식과 배열을 다루는 고차 함수들을 집중적으로 파고들었습니다. 파이썬의 리스트 컴프리헨션을 대체하는 자바스크립트의 메서드 체이닝 기법을 통해 데이터 가공의 흐름을 제어하는 훈련을 진행했습니다.

### 함수의 끌어올림과 화살표 함수
함수를 선언하는 세 가지 방식인 기명 함수 선언, 익명 함수 할당, 그리고 화살표 함수를 비교했습니다. 기명 함수의 경우 코드가 실행되기 전 메모리에 먼저 등록되는 호이스팅 현상으로 인해 선언 위치보다 위에서 호출이 가능하지만, 상수에 할당된 함수나 화살표 함수는 호이스팅의 영향을 받지 않아 실행 순서를 예측하기 더 쉽다는 구조적 이점을 확인했습니다. 특히 화살표 함수는 중괄호와 리턴 키워드를 생략하여 식을 극도로 간결하게 만들 수 있어, 이후 배열 메서드의 콜백 함수로 활용할 때 가독성을 획기적으로 높여주었습니다.

### 데이터 파이프라인의 구축
파이썬에서 리스트 내부의 요소를 가공할 때 리스트 내포 문법을 사용했던 것과 달리, 자바스크립트에서는 map, filter, forEach라는 고차 함수를 활용하여 데이터의 흐름을 파이프라인처럼 구축했습니다. 

원본 배열의 가격 데이터를 10퍼센트 인상된 새로운 배열로 매핑하고, 그중에서 2000 이상의 값만 필터링하여 남긴 뒤, 최종적으로 포이치 메서드를 통해 화면에 개별 출력하는 일련의 과정을 실습했습니다. 데이터를 담고 있는 배열 객체 자체가 자신의 요소를 가공할 수 있는 메서드를 내장하고 있으며, 여기에 화살표 함수를 주입하여 로직을 완성하는 방식은 철저하게 객체지향적이고 함수형 프로그래밍의 패러다임이 융합된 결과물임을 이해했습니다.

```javascript
// 파이썬의 리스트 컴프리헨션을 대체하는 자바스크립트의 배열 고차 함수 활용
// 원본 데이터를 훼손하지 않고 새로운 배열을 반환하는 불변성의 원칙을 지킵니다.

const prices = [1000, 2500, 500, 3000];

// 1. 데이터의 일괄 가공 (파이썬: [p * 1.1 for p in prices])
// 화살표 함수의 암묵적 반환을 활용하여 코드를 간결하게 구성합니다.
const raisedPrices = prices.map(p => p * 1.1);

// 2. 조건에 따른 데이터 정제 (파이썬: [p for p in raised_prices if p >= 2000])
const expensiveItems = raisedPrices.filter(p => p >= 2000);

// 3. 정제된 데이터의 소비 (파이썬: for item in expensive_items: print(...))
expensiveItems.forEach(p => console.log(`비싼 상품 가격: ${p}`));