# [SeSAC 도봉] 13일차 : 스키마의 진화와 데이터 흐름의 통제

서비스가 성장함에 따라 데이터의 구조는 필연적으로 변화하며, 시스템은 늘어나는 트래픽의 흐름을 효율적으로 제어해야 합니다. 오늘은 알렘빅을 도입하여 데이터베이스 스키마의 형상을 안전하게 관리하는 방법을 익히고, 알고리즘 훈련을 통해 덱 자료구조를 활용한 프로세스의 시뮬레이션을 구현했습니다. 더불어 처음으로 도입해 본 AI 기반 에디터 커서를 통해, 현대 개발자에게 요구되는 역량이 단순한 코드 타이핑이 아닌 시스템 아키텍처의 설계와 검증으로 옮겨가고 있음을 확인한 하루였습니다.

---

## 1. 알렘빅 : 스키마의 형상 관리와 데이터 보존

기존 데이터베이스 스키마 설계 방식에서는 모델 클래스에 새로운 속성이 추가되거나 변경될 때마다 기존 테이블을 삭제하고 다시 생성해야 하는 치명적인 한계가 있었습니다. 이는 서비스가 운영 중이거나 보존해야 할 중요한 비즈니스 데이터가 존재할 때 막대한 손실과 장애를 초래합니다. 

### 마이그레이션을 통한 연속성 확보
이를 해결하기 위해 데이터베이스 마이그레이션 도구인 알렘빅을 학습하고 프로젝트에 적용했습니다. 깃이 소스 코드의 변경 이력을 추적하듯, 알렘빅은 데이터베이스 스키마의 변천사를 버전으로 관리합니다. 사용자 테이블에 닉네임 컬럼을 추가하는 마이그레이션 스크립트를 생성하고 적용하는 과정을 거치며, 업그레이드와 다운그레이드 함수의 역할을 명확히 이해했습니다. 데이터의 구조가 변경되더라도 기존 레코드를 안전하게 보존하면서 시스템을 진화시킬 수 있다는 사실은, 백엔드 개발자로서 서비스의 연속성을 보장하는 가장 강력한 방어책임을 깨달았습니다.

---

## 2. 오늘의 집중 학습 : 덱을 활용한 시뮬레이션과 AI의 도입

오후에는 파이썬 내장 모듈인 컬렉션스의 덱을 활용하여 선입선출 기반의 데이터 흐름을 제어하는 알고리즘 훈련을 진행했으며, 이 과정 전반에 커서 에디터를 활용하여 생산성의 변화를 관찰했습니다.

### 자료구조의 선택이 결정하는 시간 복잡도
단순한 리스트의 첫 번째 요소를 제거하는 연산은 나머지 모든 요소의 인덱스를 재배열해야 하므로 데이터가 커질수록 치명적인 성능 저하를 일으킵니다. 반면 덱의 팝레프트는 데이터의 이동 없이 연결 리스트의 포인터만 변경하여 양방향에서 상수 시간으로 데이터를 추출할 수 있음을 실제 성능 차이로 체감했습니다. 

프린터 큐 시뮬레이션 문제를 해결하며, 단순히 맨 앞의 문서를 뽑는 것을 넘어 큐 내부의 최대 중요도를 판별하고 조건에 맞지 않으면 다시 큐의 맨 뒤로 삽입하는 논리적 흐름을 코드로 구현했습니다. 이 과정에서 내가 추적해야 하는 특정 문서의 위치 정보를 튜플 형태로 우선순위 데이터와 함께 묶어 큐에 적재하는 기법의 유용성을 절감했습니다.

### 커서 도입이 가져온 역할의 변화
오늘의 또 다른 큰 성과는 AI 기반 코드 에디터인 커서를 실무 학습 환경에 도입해 본 것입니다. 기존에는 공식 문서를 뒤적이며 문법의 세부 사항을 맞추는 데 많은 리소스를 소모했다면, 이제는 도메인의 요구사항을 명확한 프롬프트로 정의하고 AI가 제안한 로직의 결함을 찾아내는 아키텍트이자 리뷰어로 나의 역할이 진화하고 있음을 확인했습니다. 하지만 알렘빅의 설정이나 큐의 순회 로직을 자동 완성의 도움을 받아 작성하면서도, 그 기반에는 그동안 치열하게 고민하며 쌓아온 자료구조와 프레임워크에 대한 깊은 이해가 전제되어야 비로소 생성된 코드를 온전히 통제하고 시스템에 병합할 수 있음을 다시 한번 확신하게 되었습니다.

```python
# 덱을 활용한 우선순위 시뮬레이션 (프린터 큐 논리 제어)
from collections import deque

def solution_printer(priorities, location):
    # 문서의 중요도와 초기 인덱스 위치를 튜플로 묶어 덱에 초기화합니다.
    # 이는 문서를 뒤로 재배치하더라도 추적 대상(location)을 잃어버리지 않기 위함입니다.
    queue = deque([(priority, idx) for idx, priority in enumerate(priorities)])
    count = 0

    while queue:
        current = queue.popleft()
        
        # 큐에 남은 대기열 중 현재 뽑은 문서보다 중요도가 높은 문서가 존재하는지 검증합니다.
        if queue and max(queue)[0] > current[0]:
            # 더 높은 중요도가 있다면 현재 문서를 인쇄하지 않고 큐의 맨 뒤로 이동시킵니다.
            queue.append(current)
        else:
            # 우선순위가 가장 높다면 문서를 인쇄 처리합니다.
            count += 1
            
            # 방금 인쇄한 문서의 초기 인덱스가 내가 찾고자 했던 문서의 위치와 일치하는지 대조합니다.
            if current[1] == location:
                return count