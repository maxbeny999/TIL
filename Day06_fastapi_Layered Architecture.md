# [SeSAC 도봉] 2월 5일 : 데이터의 영속성과 아키텍처의 분리

파이썬의 기초 문법을 넘어 데이터가 실제로 저장되고 관리되는 원리를 탐구했습니다. 휘발성 메모리의 한계를 극복하기 위한 파일 입출력부터, 웹 애플리케이션의 규모가 커짐에 따라 필연적으로 요구되는 구조적 설계를 FastAPI를 통해 경험했습니다. 단순한 기능 구현을 넘어, 시스템이 데이터를 어떻게 다루고 유지보수성을 확보해야 하는지에 대한 본질적인 고민을 담았습니다.

---

## 1. 데이터의 흐름과 영속성 (Persistence)

오전에는 파이썬의 심화 과제인 Day 10과 Day 11 내용을 통해 파일 입출력과 복합 자료구조를 다루는 로직을 점검했습니다.

### 휘발성 극복과 상태 저장
프로그램이 종료되면 사라지는 메모리 변수의 한계를 넘기 위해 파일 입출력(File I/O)을 학습했습니다. 단순히 텍스트를 저장하는 행위를 넘어, open, write, close로 이어지는 리소스 관리의 중요성을 확인했습니다. 특히 Diary 클래스를 구현하며 객체 내부의 데이터를 외부 파일로 내보내는 과정은 데이터베이스의 가장 원시적인 형태임을 이해할 수 있었습니다. 날짜와 시간을 다루는 datetime 모듈을 결합하여 데이터의 생성 시점을 기록하는 법 또한 익혔습니다.

### 비즈니스 로직의 구현
Day 11의 과제였던 딕셔너리와 리스트의 복합 활용은 실제 현업 데이터 처리와 매우 유사했습니다. 재고 자산의 가치를 계산하거나 서버 로그에서 에러 레벨을 필터링하는 문제는 단순한 문법 암기가 아닌, 데이터의 구조를 파악하고 원하는 정보만 추출해내는 논리적 사고를 요구했습니다. 특히 수강생을 반별로 그룹핑하는 문제에서 딕셔너리의 키 존재 여부를 확인하고 리스트를 할당하는 패턴은 데이터 전처리의 핵심 로직임을 깨달았습니다.

---

## 2. FastAPI : 모듈화와 책임의 분리

오후에는 Product, TodoList, Mysite4 등 여러 예제를 실습하며 FastAPI의 구조적 설계에 집중했습니다. 코드가 길어질수록 하나의 파일(main.py)에 모든 로직을 담는 것이 얼마나 비효율적인지 체감했습니다.

### 라우터(Router)를 통한 계층 구조화
APIRouter를 도입하여 기능별로 파일을 분리하는 작업을 수행했습니다. main.py는 애플리케이션의 진입점 역할만 수행하고, 실제 상품(Product)이나 할 일(Todo)에 대한 처리는 각각의 라우터 파일로 위임했습니다. 이는 코드의 가독성을 높일 뿐만 아니라, 각 모듈이 독립적으로 기능하게 함으로써 협업과 유지보수에 유리한 아키텍처임을 확인했습니다.

### 서비스(Service) 계층의 도입
Mysite4 실습을 통해 라우터에서 비즈니스 로직을 분리해내는 '서비스 계층' 패턴을 적용했습니다. 라우터는 오직 '요청을 받고 응답을 주는 것'에만 집중하고, 실제 데이터를 가공하거나 저장하는 무거운 작업은 서비스 모듈로 이관했습니다. 이로써 코드는 하나의 거대한 덩어리가 아닌, 각자의 역할이 명확한 부품들의 조립으로 재탄생했습니다. 설계란 단순히 파일을 나누는 것이 아니라, 각 코드의 책임을 명확히 규정하는 과정임을 절감했습니다.

```python
# [FastAPI 구조화 예시] : 라우터와 서비스의 책임 분리

# 1. router.py (컨트롤러 역할)
# 요청을 수신하고, 데이터 형식을 검증하며, 적절한 서비스 함수를 호출합니다.
from fastapi import APIRouter
from mysite4.services import post_service
from mysite4.schemas import PostCreate

router = APIRouter(prefix="/posts")

@router.post("/", status_code=201)
def create_post(post_input: PostCreate):
    # 라우터는 '어떻게' 저장하는지 모릅니다. 단지 서비스에게 요청할 뿐입니다.
    new_post = post_service.add_post(post_input)
    return new_post


# 2. service.py (비즈니스 로직 역할)
# 실제 데이터의 가공, 계산, DB 저장 등의 핵심 로직을 수행합니다.
def add_post(data: PostCreate):
    # 데이터베이스 대용의 딕셔너리 리스트에 데이터를 추가합니다.
    # 만약 DB가 변경되더라도 라우터 코드는 수정할 필요가 없습니다.
    post_dict = data.model_dump()
    fake_db.append(post_dict)
    return post_dict

# 이처럼 계층을 분리함으로써 유지보수성과 코드의 재사용성을 극대화합니다.