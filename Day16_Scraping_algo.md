# [SeSAC 도봉] 16일차 : 자료구조의 확장과 그리디 알고리즘 적용

알고리즘은 단순히 복잡한 수학 문제를 푸는 과정이 아니라 현실의 제약을 소프트웨어적으로 극복하는 논리의 훈련임을 확인했습니다. 오늘은 해시맵과 스택이라는 고전적인 자료구조를 응용하여 데이터의 흐름을 통제하고 매 순간 최적의 해를 찾아가는 그리디 알고리즘의 원리를 깊이 있게 탐구했습니다. 아울러 파이썬을 활용해 구글 뉴스의 웹 데이터를 수집하는 스크래핑을 실습하며, 외부의 비정형 데이터를 시스템 내부의 정형화된 구조로 끌어오는 데이터 파이프라인의 첫 단추를 꿰었습니다. 직관에 의존하던 과거의 코딩 습관을 버리고 주어진 상황에 가장 적합한 도구를 선택하는 아키텍트의 관점을 기르는 시간이었습니다.

---

## 1. 해시맵과 스택을 통한 데이터 통제

데이터의 상태를 추적하고 가공하는 과정에서 어떤 자료구조를 선택하느냐에 따라 코드의 우아함과 시스템의 성능이 결정됨을 확인했습니다.

### 선언적 코드를 통한 논리의 추상화
파이썬 내장 모듈인 컬렉션스의 카운터를 활용하여 데이터의 빈도를 집계하는 과정을 실습했습니다. 이전에는 빈 딕셔너리를 생성하고 반복문과 조건문을 통해 데이터의 존재 여부를 일일이 확인해야 했습니다. 하지만 카운터 객체를 도입함으로써 이러한 절차적 코드를 단 한 줄의 선언적 코드로 대체할 수 있었습니다. 특히 두 집합 간의 포함 관계를 검증할 때 카운터 객체 간의 뺄셈 연산을 적용하는 기법은 내부적인 해시 충돌이나 반복 순회의 부담을 덜어주며 수학적 집합의 개념을 코드에 그대로 투영하는 논리적 추상화의 결과물임을 깨달았습니다.

### 과거의 상태를 기억하는 스택의 원리
선입후출 구조인 스택을 활용하여 연속적으로 중복되는 문자를 제거하는 로직도 구현했습니다. 새로운 데이터를 리스트에 무작정 추가하는 대신 스택의 최상단 데이터와 현재 데이터를 대조하여 다를 때만 적재하는 방식입니다. 이는 과거의 상태를 기억하고 현재의 입력에 따라 미래의 상태를 결정하는 일종의 상태 머신과 같은 역할을 수행하며 메모리의 낭비 없이 데이터의 정합성을 유지하는 견고한 방법임을 절감했습니다.

---

## 2. 비정형 데이터의 구조화와 웹 스크래핑

내부 시스템에서 정제된 데이터를 다루는 것을 넘어 외부 웹 생태계에 흩어져 있는 비정형 데이터를 수집하고 가공하는 구글 뉴스 스크래핑을 실습했습니다. 

### 문서 객체 모델의 탐색과 파싱
웹 페이지의 소스코드를 단순한 문자열로 취급하지 않고 부모와 자식 노드로 이루어진 거대한 트리 구조로 해석하는 파싱의 원리를 확인했습니다. 외부로 통신을 요청하고 뷰티풀수프 라이브러리를 통해 문서 객체를 순회하는 과정은 결국 앞서 학습한 트리 자료구조의 탐색 알고리즘이 실제 환경에서 어떻게 응용되는지를 보여주는 완벽한 사례였습니다. 뉴스 기사의 제목과 하이퍼링크를 감싸고 있는 특정 태그와 클래스를 식별하고 이를 기반으로 데이터를 추출해 내며 프론트엔드의 시각적 마크업이 백엔드의 데이터 소스로 변환되는 접점을 이해했습니다.

### 외부 의존성 통제와 방어적 수집
스크래핑 로직을 작성하며 외부 환경의 변화에 대비하는 방어적 프로그래밍의 중요성도 다시 한번 확인했습니다. 대상 웹 사이트의 구조는 언제든 변경될 수 있으며 특정 기사에는 예상했던 태그나 속성이 누락되어 있을 수 있습니다. 따라서 데이터를 추출할 때 발생할 수 있는 속성 에러나 인덱스 에러를 트라이 익셉트 구문으로 감싸고 안전한 기본값을 할당하는 로직을 추가하여 시스템이 예기치 않게 중단되는 현상을 방지했습니다.

---

## 3. 오늘의 집중 학습 : 그리디의 본질과 정렬의 논리

오후에는 그리디 알고리즘의 기초를 다루며 거스름돈 최소화 문제와 로프의 최대 중량 계산 문제를 해결했습니다. 그리디 방식은 모든 경우의 수를 탐색하지 않고 현재 상황에서 가장 이득이 되는 선택을 반복하여 최종 해답에 도달하는 기법입니다.

### 정렬을 통한 최적 부분 구조의 완성
특히 여러 개의 로프를 병렬로 연결하여 들어 올릴 수 있는 최대 중량을 구하는 문제에서 정렬의 중요성을 깊이 체감했습니다. 모든 조합을 계산하여 메모리를 낭비하는 대신 로프가 버틸 수 있는 중량을 내림차순으로 정렬한 뒤 가장 튼튼한 로프부터 하나씩 추가하며 최대 하중을 갱신하는 논리입니다. 

병렬로 연결된 로프들은 가장 약한 로프의 버티는 힘에 맞추어 하중을 분담하게 됩니다. 따라서 현재 선택된 로프 중 가장 약한 로프의 하중에 연결된 로프의 총 개수를 곱하는 공식을 도출할 수 있었습니다. 데이터의 규칙을 발견하고 이를 정렬과 결합하여 최적의 경로를 찾아내는 이 과정은 백엔드 시스템에서 병목 현상을 해결하고 쿼리 실행 계획을 최적화하는 맥락과도 맞닿아 있음을 이해했습니다.

```python
# 1. 그리디 알고리즘과 정렬을 활용한 로프의 최대 중량 계산
def calculate_max_weight(ropes: list) -> int:
    # 로프를 버틸 수 있는 하중이 큰 순서대로 내림차순 정렬하여 탐색의 효율성을 높입니다.
    ropes.sort(reverse=True)
    max_weight = 0

    # 병렬 연결 시 전체 하중은 가장 약한 로프의 하중과 로프의 총 개수의 곱으로 결정됩니다.
    for index, weight in enumerate(ropes):
        current_weight = weight * (index + 1)
        
        if current_weight > max_weight:
            max_weight = current_weight

    return max_weight

# 2. 방어적 로직이 적용된 웹 스크래핑 개념
def scrape_google_news(html_document):
    news_data = []
    # HTML 문서를 파싱하여 구조화된 객체로 변환합니다.
    soup = BeautifulSoup(html_document, "html.parser")
    
    # 기사를 감싸고 있는 태그를 순회하며 데이터를 추출합니다.
    for article in soup.find_all("article"):
        try:
            # 외부 구조 변화나 태그 누락에 대비한 안전한 탐색을 진행합니다.
            title_tag = article.find("h3")
            link_tag = article.find("a")
            
            if title_tag and link_tag:
                news_data.append({
                    "title": title_tag.get_text(strip=True),
                    "link": link_tag.get("href")
                })
        except AttributeError:
            continue
            
    return news_data