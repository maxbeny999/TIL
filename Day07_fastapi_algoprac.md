# [SeSAC 도봉] 7일차 : 데이터의 영속성과 알고리즘적 사고의 심화

프로그램이 종료되면 사라지는 휘발성 데이터의 한계를 넘기 위해, 애플리케이션과 데이터베이스를 연결하는 영속성(Persistence)의 세계로 진입했습니다. 이 과정에서 프레임워크가 제공하는 편의 기능 이면에 숨겨진 제어의 흐름을 파악했습니다. 또한, 난이도 있는 알고리즘 문제들을 연속으로 해결하며 자료구조를 적재적소에 활용하는 논리력을 한 단계 끌어올린 하루였습니다.

---

## 1. FastAPI와 데이터베이스 : 자원의 효율적 관리

이전까지 리스트나 딕셔너리로 관리하던 데이터는 서버가 재시작되면 소실되는 한계가 있었습니다. 이를 해결하기 위해 RDBMS를 연동하며, 시스템이 데이터를 다루는 방식의 근본적인 변화를 확인했습니다.

### 세션 생명주기의 제어 (Context Management)
가장 인상 깊었던 지점은 `database.py`에서 정의한 `get_db` 함수의 동작 방식입니다. 파이썬의 제너레이터 문법인 `yield`를 활용하여, 요청(Request)이 들어올 때 세션을 생성하고 응답(Response)이 완료되면 `finally` 블록을 통해 반드시 세션을 닫는 구조를 구현했습니다. 이는 개발자가 매번 커넥션을 열고 닫는 번거로움에서 벗어나게 할 뿐만 아니라, 커넥션 누수(Leak)를 원천적으로 차단하는 안전장치임을 이해했습니다.

### 의존성 주입 (Dependency Injection)과 결합도
라우터 함수에서 `db: Session = Depends(get_db)` 구문을 통해 DB 세션을 주입받는 패턴은, 객체 지향의 의존성 주입(DI) 원칙을 웹 프레임워크에 적용한 사례였습니다. 비즈니스 로직이 데이터베이스 연결 객체를 직접 생성하지 않고 외부에서 주입받음으로써, 모듈 간의 결합도(Coupling)를 낮추고 유닛 테스트가 용이한 유연한 설계를 확보할 수 있음을 확인했습니다.

---

## 2. 개인 학습 : 실버 난이도 알고리즘 정복

오후에는 자료구조와 정렬 로직을 활용해야 하는 실버 레벨의 알고리즘 문제 3개를 풀며 문제 해결 능력을 점검했습니다.

### 스택(Stack) : 선형 데이터의 조건부 처리
괄호의 짝을 맞추는 문제(BOJ 9012 등)를 해결하며 스택 자료구조의 유용성을 체감했습니다. 문자열을 순차적으로 탐색하며 여는 괄호는 저장(Push)하고, 닫는 괄호는 가장 최근의 데이터와 상쇄(Pop)시키는 LIFO(Last-In-First-Out) 방식을 적용했습니다. 이를 통해 단순히 개수를 세는 방식보다 훨씬 직관적이고 정확하게 정합성을 검증할 수 있었습니다.

### 복합 정렬과 람다(Lambda)
다양한 조건을 가진 데이터를 정렬하는 문제를 해결했습니다. 딕셔너리 리스트를 '점수 내림차순', 점수가 같다면 '이름 오름차순'으로 정렬해야 할 때, 파이썬의 `sort` 함수와 `lambda` 표현식을 결합하여 해결했습니다. `key=lambda x: (-x['score'], x['name'])`와 같이 튜플을 활용해 우선순위를 지정하는 테크닉을 익히며, 간결하면서도 강력한 파이썬의 문법적 특성을 다시금 확인했습니다.

### 성취와 회고
오늘 실버 난이도 문제 3개를 연달아 해결하면서, 단순히 문법을 아는 단계를 넘어 문제의 요구사항을 논리적인 코드로 변환하는 '구현력'이 향상되었음을 느꼈습니다. 특히 코드를 작성하기 전, 어떤 자료구조를 사용할지 먼저 설계하는 습관이 디버깅 시간을 단축시키는 핵심임을 확신하게 되었습니다.

```python
# [Algorithm] : 람다를 활용한 다중 조건 정렬
# 요구사항: 1. 점수가 높은 순서(내림차순) 2. 점수가 같다면 이름 순서(오름차순)

students = [
    {"name": "Alice", "score": 88},
    {"name": "Bob", "score": 95},
    {"name": "Charlie", "score": 70},
    {"name": "David", "score": 95}
]

# 튜플의 첫 번째 요소는 내림차순(-) 처리, 두 번째 요소는 기본 오름차순
sorted_students = sorted(students, key=lambda x: (-x['score'], x['name']))

# 결과: Bob(95) -> David(95) -> Alice(88) -> Charlie(70)
# Bob과 David는 점수가 같으므로 이름 사전순으로 정렬됨g