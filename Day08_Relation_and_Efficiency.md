# [SeSAC 도봉] 8일차 : 관계의 정의와 효율적인 데이터 처리

데이터베이스 설계의 핵심은 개별 데이터의 정의가 아니라, 데이터 사이의 관계를 어떻게 규명하느냐에 달려 있음을 확인했습니다. 지금까지는 단일 테이블이나 단순한 1:N 관계를 다루었다면, 오늘은 사용자(User)와 상품(Product)이라는 두 주체가 위시리스트(Wishlist)라는 매개체를 통해 다대다(N:M)로 연결되는 복잡한 구조를 설계하고 구현했습니다. 또한, 알고리즘 풀이를 통해 자료구조의 선택이 프로그램의 성능에 미치는 영향을 다시금 체감하는 시간을 가졌습니다.

---

## 1. 다대다 관계의 해소와 연결 모델

현실 세계에서 한 명의 사용자는 여러 상품을 찜할 수 있고, 하나의 상품은 여러 사용자에게 찜을 당할 수 있습니다. 이러한 다대다 관계를 관계형 데이터베이스에서 직접 표현하는 것은 불가능하며, 이를 해결하기 위해 연결 테이블(Association Table)을 도입해야 함을 배웠습니다.

### 위시리스트 : 관계의 실체화

단순히 사용자와 상품을 연결하는 것을 넘어, 위시리스트라는 중간 모델을 정의함으로써 관계 자체가 하나의 실체가 됨을 이해했습니다. 예를 들어 `created_at` 컬럼을 추가함으로써 사용자가 언제 이 상품을 찜했는지에 대한 이력 관리가 가능해집니다. 이는 데이터베이스 정규화 과정에서 제3 정규형을 만족시키기 위한 필수적인 조치이자, 비즈니스 로직을 풍성하게 만드는 기반이 됩니다.

또한 `Association Proxy` 패턴을 학습하며 코드의 가독성을 높이는 방법을 고민했습니다. `user.wishlist_items.append(product)`와 같이 직관적인 코드로 복잡한 관계 데이터를 조작할 수 있다는 점은, 내부의 복잡성을 감추고 개발자에게 단순한 인터페이스를 제공하는 추상화의 미학이라 생각합니다.

### API 설계와 예외 처리

사용자 생성부터 위시리스트 추가, 조회, 삭제로 이어지는 API 엔드포인트를 설계하며 데이터 무결성에 대한 고민을 했습니다. 특히 위시리스트에 상품을 추가할 때, 이미 찜한 상품인지 검증하는 로직은 데이터의 중복을 막기 위해 필수적입니다. 단순히 기능을 구현하는 것에 그치지 않고, 발생할 수 있는 예외 상황을 미리 예측하고 방어하는 코드를 작성하는 것이 백엔드 개발자의 책무임을 다시 한번 절감했습니다.

---

## 2. 오늘의 집중 학습 : 자료구조의 효율성 증명

오후에는 백준 2164번 카드2 문제를 해결하며 큐(Queue) 자료구조의 효율성을 검증했습니다. 이전에 학습했던 리스트와 덱(Deque)의 성능 차이를 이론이 아닌 실제 문제 풀이를 통해 확신하는 계기가 되었습니다.

### 논리적 사고와 시간 복잡도

문제의 로직은 단순합니다. 맨 위의 카드를 버리고, 그 다음 카드를 맨 아래로 옮기는 행위를 카드가 한 장 남을 때까지 반복하는 것입니다. 이 과정을 파이썬의 리스트(List)로 구현할 경우 `pop(0)` 연산이 수행될 때마다 나머지 원소들을 앞으로 당겨야 하므로 O(N)의 시간이 소요됩니다. 전체 로직의 시간 복잡도가 O(N^2)이 되어 시간 초과가 발생함을 확인했습니다.

반면 `collections` 모듈의 `deque`를 사용하면 양쪽 끝에서의 삽입과 삭제가 O(1)에 수행되므로, 전체 로직을 O(N)으로 최적화할 수 있습니다. 동일한 논리라도 어떤 그릇에 데이터를 담느냐에 따라 결과가 완전히 달라질 수 있음을 확인했습니다.

```python
# 백준 2164 : 카드2
# 리스트가 아닌 덱(Deque)을 사용하여 시간 복잡도를 O(N)으로 최적화

from collections import deque
import sys

def solve_card_problem():
    # 입력값을 받아 정수로 변환
    try:
        n = int(sys.stdin.readline())
    except ValueError:
        return

    # 1부터 N까지의 숫자를 덱에 초기화
    # 덱은 양방향에서 데이터 추가/삭제가 O(1)로 가능함
    queue = deque(range(1, n + 1))

    # 카드가 한 장 남을 때까지 반복
    while len(queue) > 1:
        # 1. 맨 위의 카드를 버림
        queue.popleft()
        
        # 2. 그 다음 카드를 맨 아래로 이동
        # popleft로 꺼낸 값을 다시 append로 넣음
        queue.append(queue.popleft())

    # 마지막 남은 카드 출력
    print(queue[0])

if __name__ == "__main__":
    solve_card_problem()