# [SeSAC 도봉] 15일차 : 데이터의 방어적 제어와 시각적 레이아웃의 구조화

백엔드에서 다루는 데이터의 구조가 복잡해질수록 이를 안전하고 효율적으로 제어하는 능력이 시스템의 안정성을 좌우함을 절감했습니다. 오늘은 파이썬의 심화된 자료구조와 내장 모듈을 활용하여 예측 불가능한 외부 데이터를 정교하게 가공하는 훈련을 진행했습니다. 아울러 웹의 시각적 뼈대를 구성하는 CSS의 포지셔닝과 플렉스박스 모델을 직접 구현하며, 서버에서 전달된 데이터가 클라이언트의 화면에 어떠한 논리로 배치되고 렌더링되는지 그 맥락을 연결하는 시간을 가졌습니다.

---

## 1. 파이썬 코어 로직 : 예외의 통제와 효율적 집계

외부 API와 통신하거나 복잡한 비즈니스 로직을 처리할 때, 데이터는 항상 우리가 기대하는 이상적인 형태로 주어지지 않습니다. 오늘 진행한 파이썬 데이터 제어 훈련은 단순한 문법 체득을 넘어, 시스템의 장애를 유발할 수 있는 예외 상황을 구조적으로 차단하는 방어적 프로그래밍에 초점이 맞춰져 있었습니다.

### 키 에러의 구조적 예방과 기본값 딕셔너리
서버의 응답 상태 코드를 기준으로 수많은 로그 데이터를 그룹화하는 로직을 구현했습니다. 기존의 방식이라면 딕셔너리에 해당 상태 코드가 키로 존재하는지 매번 조건문으로 확인하고 빈 리스트를 초기화하는 번거로운 과정을 거쳐야 했습니다. 하지만 파이썬 내장 모듈인 컬렉션스의 기본값 딕셔너리를 도입함으로써, 키의 존재 여부를 검증하는 로직을 제거하고 즉시 값을 추가하는 우아한 제어 흐름을 완성했습니다. 이는 코드의 가독성을 높일 뿐만 아니라 불필요한 연산을 줄여 성능을 최적화하는 핵심 기법임을 확인했습니다.

### 중첩 JSON 데이터의 안전한 탐색
또한 깊게 중첩된 외부 API의 응답 데이터에서 사용자의 이메일을 추출하는 과정을 실습했습니다. 프로필이나 연락처 정보가 누락된 불량 데이터에 대괄호 접근법을 사용하면 즉각적으로 시스템이 중단되는 키 에러가 발생합니다. 이를 방지하기 위해 딕셔너리의 겟 메서드를 체이닝하여 연속적으로 호출하고, 중간 단계의 키가 없을 경우 빈 딕셔너리를 기본값으로 반환하게 설정함으로써 어떠한 상황에서도 에러 없이 종단점의 데이터에 접근하거나 널 값을 안전하게 반환받는 논리를 체득했습니다.

---

## 2. 오늘의 집중 학습 : CSS 플렉스박스를 통한 공간의 논리적 분할

오후에는 백엔드의 관점을 잠시 벗어나, 클라이언트 측에서 요소들을 배치하는 CSS의 포지셔닝과 플렉스 레이아웃을 집중적으로 다루었습니다. 화면을 구성하는 작업 역시 단순한 시각적 치장이 아니라 수학적이고 논리적인 공간 분할의 영역임을 이해하게 되었습니다.

### 문서 흐름의 제어와 포지셔닝
요소들을 화면에 띄우기 위해 CSS의 포지션 속성을 다루었습니다. 기본적으로 HTML 요소들이 위에서 아래로 쌓이는 정상적인 문서 흐름을 이해하고, 이를 기준으로 릴러티브 속성을 부여하여 자기 자신의 원래 위치를 기준으로 좌표를 이동시키는 원리를 확인했습니다. 이는 요소 간의 겹침 현상을 제어하고 복잡한 UI 컴포넌트의 기준점을 설정하는 데 필수적인 개념이었습니다.

### 플렉스박스 : 선형 레이아웃의 선언적 제어
가장 인상 깊었던 것은 플렉스박스 모델의 도입이었습니다. 과거의 부자연스러운 레이아웃 제어 방식을 탈피하여, 부모 컨테이너에 플렉스 속성을 부여하는 것만으로 자식 요소들의 정렬과 공간 배분을 유연하게 통제할 수 있었습니다.

특히 HTML 마크업 단계에서부터 중앙 정렬과 같은 유틸리티 클래스를 미리 정의해 두고 조합하여 사용하는 방식을 실습했습니다. 세 개의 박스를 가로로 균등하게 배치하거나, 열 방향으로 전환하여 수직 정렬을 맞추는 등 복잡한 그리드 형태의 구조를 CSS 파일의 수정 없이 HTML 클래스 선언만으로 조작해 냈습니다. 이는 프론트엔드 개발에서 컴포넌트의 재사용성을 높이고 유지보수를 용이하게 만드는 현대적인 설계 패러다임과 맞닿아 있음을 확인했습니다.

```python
# [Python] 딕셔너리의 안전한 탐색 및 그룹화 (Day 18 실습)
from collections import defaultdict

# 1. 상태 코드별 그룹화 로직 (defaultdict 활용)
# 키가 존재하지 않을 때 KeyError를 발생시키는 대신 빈 리스트를 자동으로 생성합니다.
group_code = defaultdict(list)
for data in logs:
    group_code[data['status']].append(data['id'])

# 2. 중첩 JSON의 체이닝 탐색 (get 메서드 방어적 활용)
# 중간 단계의 키가 누락되어도 에러 없이 기본값(빈 딕셔너리)을 반환하며 안전하게 통과합니다.
user_email = api_response["data"]['user'].get('profile', {}).get('contact', {}).get('email')

# 3. 2차원 리스트의 순회와 인덱스 추출 (enumerate 활용)
# 별도의 카운터 변수 없이 행(일차)과 열(시간대)의 인덱스를 즉시 튜플로 묶어냅니다.
available_times = [
    (day_idx, time_idx) 
    for day_idx, day_schedule in enumerate(schedule) 
    for time_idx, is_booked in enumerate(day_schedule) 
    if is_booked == 0
]