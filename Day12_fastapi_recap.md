# [SeSAC 도봉] 12일차 : 다대다 관계의 설계와 데이터 핸들링의 본질

긴 설 연휴를 마치고 다시 시스템의 논리적 흐름 속으로 복귀했습니다. 휴식기로 인해 굳어진 사고를 유연하게 만들기 위해 파이썬의 핵심인 데이터 가공 훈련을 진행하는 한편, 개인 복습용 미니 프로젝트인 넷플릭스 클론에 영화와 배우 사이의 다대다 관계를 새롭게 정의하고 연결하는 작업을 수행했습니다. 기초 문법의 탄탄함이 결국 웹 프레임워크의 활용도로 직결된다는 사실을 다시금 절감한 하루였습니다.

---

## 1. 다대다 관계의 구조적 해소

영화와 배우의 관계는 전형적인 다대다 구조를 가집니다. 하나의 영화에는 여러 배우가 출연하며, 한 명의 배우 역시 여러 영화에 캐스팅될 수 있습니다. 이를 관계형 데이터베이스에서 구현하기 위해 중간 매개체 역할을 하는 연결 테이블을 도입해야 함을 배웠습니다.

### 순수 테이블 객체를 통한 매핑
이전 프로젝트에서 사용했던 모델 클래스 방식의 연결 테이블과 달리, 이번에는 데이터베이스 메타데이터에 종속되는 순수 테이블 객체를 생성하여 관계를 묶어주었습니다. 식별자 필드 두 개만을 기본키이자 외래키로 가지는 출석부 역할의 테이블을 정의하고, 영화와 배우 모델 양쪽에서 세컨더리 속성으로 이를 참조하게 만들었습니다. 이는 추가적인 비즈니스 로직이나 생성 시간 같은 메타 데이터가 필요 없는 단순 연결 상태에서는 불필요한 클래스 선언을 줄이고 구조를 단순화할 수 있는 효율적인 접근법임을 이해했습니다.

### 안전한 데이터 연결과 검증
서비스 계층에서 특정 영화에 배우를 캐스팅하는 기능을 구현하며, 단순히 데이터를 연결하는 것을 넘어 관계를 맺기 전 데이터의 실존 여부를 검증하는 절차를 명확히 했습니다. 요청으로 들어온 식별자를 연결 테이블에 무작정 밀어 넣는 것이 아니라, 대상이 되는 영화와 배우가 데이터베이스에 존재하는지 레포지토리 계층을 통해 각각 조회한 후 예외 처리를 거치도록 로직을 작성했습니다. 이는 외래키 제약조건 위반으로 인한 데이터베이스 시스템의 에러를 사전에 방지하고, 클라이언트에게 명확한 실패 사유를 전달하기 위한 필수적인 방어 로직임을 확인했습니다.

---

## 2. 오늘의 집중 학습 : 순수 로직의 복구와 객체지향의 적용

연휴 동안 휘발된 감각을 되찾기 위해, 데이터베이스나 프레임워크의 개입 없이 순수하게 파이썬 내장 자료구조만을 활용하여 데이터를 가공하는 훈련에 집중했습니다. 예전에는 막연하게 느껴졌던 로직들이 이제는 명확한 형태를 갖추고 머릿속에 그려지는 것을 경험했습니다.

### 중첩 데이터의 안전한 추출과 병합
외부 응답이나 로그 등 정제되지 않은 딕셔너리와 리스트의 조합을 다루면서 예외 상황을 통제하는 방법을 복습했습니다. 특정 키가 존재하지 않을 때 프로그램이 다운되는 현상을 막기 위해, 조건문 대신 트라이 익셉트 구문을 활용하여 키 에러를 잡아내고 기본값을 반환하는 패턴을 직접 구현했습니다. 더불어 서로 다른 두 개의 리스트를 파이썬 내장 함수인 집을 통해 묶어내어 단숨에 하나의 딕셔너리로 병합하는 과정을 거치며, 언어가 제공하는 내장 기능의 강력함과 간결함을 체감했습니다.

### 검증 로직의 내재화와 경계 보호
기초 데이터 핸들링 훈련 이후에는 이를 파이단틱 모델과 결합하여 데이터 검증 과정을 객체지향적으로 풀어냈습니다. 사용자 생성 모델 내부에 필드 밸리데이터 데코레이터를 적용하여, 입력된 나이가 영 이하일 경우 즉각적으로 밸류 에러를 발생시키도록 설계했습니다. 데이터의 무결성을 서비스 로직에 도달하기 전인 시스템의 진입 단계에서부터 엄격하게 차단하는 이러한 구조는, 컨트롤러와 서비스 계층의 부담을 줄여주는 아주 효율적인 아키텍처임을 다시금 깨달았습니다.

```python
# 다대다 관계 설정을 위한 순수 매핑 테이블 및 모델 관계 정의
# 비즈니스 로직이 필요 없는 순수 연결 테이블은 Table 객체로 생성하여 최적화합니다.
movie_actor = Table(
    "movie_actor",
    Base.metadata,
    Column("movie_id", Integer, ForeignKey("movies.id"), primary_key=True),
    Column("actor_id", Integer, ForeignKey("actors.id"), primary_key=True),
)

class Actor(Base):
    __tablename__ = "actors"

    id = Column(Integer, primary_key=True, index=True)
    name = Column(String, index=True)

    # secondary 속성을 통해 별도의 클래스 선언 없이 양방향 조회가 가능하도록 설정
    movies = relationship("Movie", secondary="movie_actor", back_populates="actors")

# 서비스 계층의 방어적 로직
class MovieService:
    @staticmethod
    def cast_actor(db: Session, movie_id: int, actor_id: int):
        movie = MovieRepository.get_movie_by_id(db, movie_id)
        actor = ActorRepository.get_actor_by_id(db, actor_id)

        # 데이터베이스 에러를 프레임워크 단에서 사전 차단
        if not movie or not actor:
            raise HTTPException(status_code=404, detail="대상을 찾을 수 없습니다.")

        return MovieRepository.add_actor_to_movie(db, movie, actor)