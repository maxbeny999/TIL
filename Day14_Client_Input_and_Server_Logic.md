# [SeSAC 도봉] 14일차 : 클라이언트의 입력과 서버의 방어 로직

프론트엔드와 백엔드의 경계에서 데이터가 어떻게 생성되고 전달되는지 그 첫 관문을 확인했습니다. 사용자의 입력을 서버로 규격화하여 보내는 HTML 요소의 논리적 구조를 학습하며 클라이언트 측의 데이터 전송 방식을 이해했고, 오후에는 이렇게 넘어온 데이터를 서버 단에서 안전하게 파싱하고 처리하기 위한 파이썬의 딕셔너리 제어와 예외 처리 기법을 훈련하며 견고한 시스템을 위한 내실을 다졌습니다.

---

## 1. 데이터 전송의 규격화와 인터페이스 설계

웹 브라우저에서 서버로 데이터를 보내기 위한 가장 원시적이고 표준적인 방법인 폼 요소를 다루었습니다. 단순한 화면 구성을 넘어, 사용자의 의도가 담긴 데이터를 어떻게 구조화하여 백엔드로 전달할 것인가에 대한 규약을 정의하는 과정임을 깨달았습니다.

### 입력의 통제와 식별자의 부여
다양한 입력 태그를 실습하며 가장 주목한 부분은 name 속성의 역할입니다. 사용자가 텍스트, 비밀번호, 라디오 버튼 등을 통해 입력한 값은 최종적으로 서버에서 처리될 때 name 속성을 고유 식별자로 삼아 전달됩니다. 이는 백엔드 시스템에서 데이터를 JSON이나 딕셔너리 형태로 맵핑할 때 기준이 됨을 확인했습니다. 아울러 action 속성으로 목적지 주소를 지정하고, GET 방식과 POST 방식을 분리하여 데이터의 조회와 상태 변경을 구분하는 통신 규약의 기초를 절감했습니다.

### 논리적 그룹핑과 접근성
단순히 입력 창을 나열하는 것을 넘어, label 요소를 통해 입력 필드에 명확한 의미를 부여하고 포커스 영역을 확장하는 사용자 경험 측면을 고려했습니다. select 태그를 이용해 정해진 목록 내에서만 값을 제한하거나, hidden 타입으로 클라이언트 화면에는 노출하지 않은 채 시스템에 필요한 메타데이터를 전송하는 기법은 폼이 단순한 입력 도구가 아니라 서버의 방어 로직을 돕는 일차적인 검증 수단임을 이해하게 해주었습니다.

---

## 2. 오늘의 집중 학습 : 해시맵의 활용과 방어적 프로그래밍

클라이언트로부터 폼이나 API를 통해 전달받은 데이터는 기본적으로 신뢰할 수 없는 상태라는 전제하에 학습을 진행했습니다. 외부에서 유입된 데이터를 서버 내에서 안전하게 다루기 위한 딕셔너리 제어와 예외 처리, 그리고 이를 응용한 알고리즘 문제 해결에 집중했습니다.

### 예외의 통제와 무결성 확보
서버로 들어온 페이로드에서 특정 데이터의 존재 여부를 검증하는 로직을 구현했습니다. 딕셔너리의 대괄호 접근법을 사용할 때 발생하는 KeyError를 단순히 회피하는 것이 아니라, try except 구문을 통해 명시적으로 잡아내어 시스템의 비정상 종료를 막고 대체 흐름을 제공하는 방어적 코딩의 가치를 확인했습니다. 외부 입력에 의존하는 백엔드 환경에서는 데이터의 누락을 상수로 두고 아키텍처를 설계해야 함을 확신했습니다.

### 데이터 조작과 상태 관리
기초적인 딕셔너리 제어 능력을 바탕으로 FastAPI 라우터 내에서 가상의 데이터베이스를 조작하는 인터페이스를 구현했습니다. 경로 매개변수로 유저의 식별자를 받고, Pydantic 모델을 통해 수정될 데이터의 규격을 강제했습니다. 이 과정에서 내부 리스트의 딕셔너리 요소를 순회하며 값을 갱신하거나, 존재하지 않는 자원에 대한 접근 시 404 상태 코드와 함께 예외를 발생시켜 흐름을 통제하며 실제 비즈니스 로직이 동작하는 원리를 체득했습니다.

### 시간 복잡도의 극복을 위한 해시맵 도입
알고리즘 훈련에서는 이중 반복문으로 인한 시간 초과 한계를 딕셔너리를 활용하여 극복했습니다. 주어진 여러 숫자 카드의 개수를 구하는 문제를 풀며, 리스트의 기본 탐색 로직을 사용할 경우 발생하는 시간 복잡도를 딕셔너리 빈도 집계를 통해 획기적으로 최적화하는 과정을 거쳤습니다. 파이썬의 딕셔너리가 내부적으로 해시 테이블로 구현되어 있어 탐색에 상수 시간만이 소요된다는 이론적 지식을 코드로 증명하는 순간이었습니다.

```python
# 해시맵을 활용한 시간 복잡도 최적화 로직
# 이중 반복문을 피하고 딕셔너리의 빠른 탐색 속도를 활용하여 성능을 끌어올립니다.

from collections import Counter

def count_target_cards(cards: list, targets: list) -> list:
    # 1. 주어진 전체 카드의 빈도수를 딕셔너리 기반의 해시맵으로 단번에 집계합니다.
    card_counts = dict(Counter(cards))
    
    # 2. 타겟 숫자들을 순회하며 딕셔너리에서 빈도수를 추출합니다.
    # get 메서드를 사용하여 존재하지 않는 카드를 조회할 경우 안전하게 0을 반환하도록 방어 처리합니다.
    result = [card_counts.get(nums, 0) for nums in targets]
    
    return result