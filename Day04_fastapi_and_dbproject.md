# [SeSAC 도봉] 4일차 : 프레임워크의 도입과 견고한 로직의 구현

데이터베이스 설계를 넘어, 이제 그 데이터를 실제로 움직이게 만드는 웹 프레임워크 FastAPI의 기초를 다졌습니다. 강사님의 코드를 통해 나의 설계와 전문가의 설계 사이의 간극을 확인하는 시간을 가졌으며, 개인 학습을 통해 언어의 핵심인 객체지향과 알고리즘을 복습하며 기초 체력을 다시 한번 점검했습니다. 도구를 사용하는 법과 그 도구를 지탱하는 논리, 두 마리 토끼를 잡기 위해 노력한 하루였습니다.

---

## 1. 프로젝트 리팩토링 : 기능 구현과 구조적 설계의 차이

오전에는 어제 진행했던 미니 프로젝트(온라인 강의 사이트 DB 설계)의 결과물을 강사님의 예시 답안과 비교하고 분석하는 시간을 가졌습니다. 단순히 동작하는 것과 잘 설계된 것의 차이를 명확히 느낄 수 있었습니다.

### 비교를 통한 성찰
나의 설계가 단순히 요구사항을 만족시키는 데 급급했다면, 강사님의 코드는 확장성과 유지보수를 고려한 흔적이 역력했습니다. 특히 다대다 관계를 풀어내는 방식이나, 쿼리문 작성 시 인덱스를 고려한 효율성 측면에서 많은 배움을 얻었습니다. 내가 작성한 ERD와 API 명세가 실제 현업 레벨에서는 어떻게 다듬어져야 하는지, 단순한 코딩이 아닌 엔지니어링의 관점에서 바라보게 된 계기였습니다.

### FastAPI의 시작
이어서 파이썬 기반의 웹 프레임워크인 FastAPI의 기초를 학습했습니다. Flask나 Django에 비해 비동기 처리에 강점이 있고, Type Hint를 적극적으로 활용하여 코드의 안전성을 높여준다는 점이 인상적이었습니다. 아주 간단한 서버를 띄워보았는데, 몇 줄 안 되는 코드로 API 엔드포인트가 생성되는 것을 보며 프레임워크가 주는 생산성의 가치를 실감했습니다. 하지만 편리함에 매몰되지 않고 내부 동작 원리를 이해해야겠다는 다짐을 했습니다.

---

## 2. 오늘의 집중 학습 : 객체의 상호작용과 알고리즘적 사고

오후에는 파이썬 심화 과정인 Day 7, Day 8의 내용을 복습하며, 코드로 논리를 구현하는 훈련에 집중했습니다. 단순히 문법을 아는 것을 넘어, 객체 간의 데이터를 주고받거나 복잡한 조건을 효율적으로 처리하는 방법을 고민했습니다.

### 객체 간의 메세지 전달 (OOP)
객체 지향 프로그래밍의 핵심인 상호작용을 깊이 있게 다루었습니다. 특히 계좌 이체나 게임 캐릭터의 전투와 같은 기능을 구현할 때, 단순히 내 속성을 줄이고 상대 속성을 늘리는 것이 아니라, 상대 객체의 메서드를 호출하여 상태 변경을 요청하는 방식이 인상적이었습니다. 이는 객체가 자신의 데이터를 스스로 관리해야 한다는 캡슐화의 원칙을 지키면서도, 객체 간의 협력을 유도하는 올바른 설계 방식임을 깨달았습니다.

### 데이터 구조와 정렬의 미학
알고리즘 복습에서는 문자열 처리와 정렬 문제에 집중했습니다. sort 함수의 key 인자에 lambda를 사용하여 복합적인 정렬 조건을 우아하게 처리하는 방법을 익혔습니다. 또한 딕셔너리를 활용해 데이터를 카운팅하고 검색하는 로직을 구현하며, 해시맵(HashMap) 구조가 시간 복잡도를 줄이는 데 얼마나 강력한 도구인지 다시 한번 확인했습니다.

```python
# [OOP 예시] : 객체 간의 협력 (계좌 이체)
# 한 객체가 다른 객체의 데이터를 직접 조작하지 않고 메세지를 보냅니다.

class BankAccount:
    def __init__(self, owner, balance):
        self.owner = owner
        self.balance = balance

    def withdraw(self, amount):
        # 자신의 데이터(잔액)는 자신이 책임지고 검증합니다.
        if self.balance >= amount:
            self.balance -= amount
            return True
        return False

    def deposit(self, amount):
        self.balance += amount

    def transfer(self, target_account, amount):
        # '출금하다'라는 행동의 결과에 따라 '입금하다'를 요청합니다.
        if self.withdraw(amount):
            target_account.deposit(amount)
            return True
        return False