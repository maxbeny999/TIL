# [SeSAC 도봉] 5일차 : 파이썬 지식의 통합과 웹 프레임워크

지금까지 학습한 변수, 제어문, 함수, 그리고 클래스가 독립된 개념이 아니라 거대한 시스템을 지탱하는 부품임을 확인했습니다. FastAPI를 활용해 웹 서버를 구축하고 API를 설계하는 과정에서, 이전에 배운 모든 기초 지식이 하나의 흐름으로 연결되는 경험을 했습니다.

---

## 1. FastAPI 활용 : 구조적 설계와 데이터 통신

파이썬 기반의 웹 프레임워크인 FastAPI를 설치하고, 폴더 구조를 나누어 간단한 사이트와 RESTful API를 구현했습니다.

### 프레임워크의 디렉토리 구조
단일 파일에 모든 코드를 작성하는 방식에서 벗어나, 기능별로 폴더와 파일을 분리하는 모듈화 작업을 수행했습니다. `main.py`는 진입점 역할을 하고, 데이터의 형태를 정의하는 모델(Model)과 실제 로직을 처리하는 라우터(Router)를 분리했습니다. 이 과정에서 `import` 구문을 통해 다른 파일의 클래스와 함수를 불러오는 것이 단순한 참조가 아니라, 거대한 레고 블록을 조립하는 과정임을 깨달았습니다.

### RESTful API와 객체의 연결
클라이언트와 서버가 데이터를 주고받는 규약인 REST API를 직접 설계했습니다. 흥미로운 점은 우리가 이전에 배운 '클래스(Class)'가 API의 요청/응답 데이터 스키마(Pydantic BaseModel)로 그대로 활용된다는 사실입니다. 객체지향에서 속성을 정의하고 타입을 지정했던 연습이, 웹 통신에서 데이터의 무결성을 검증하는 핵심 로직으로 이어짐을 확인했습니다. 결국 웹 개발이란 HTTP라는 통신 프로토콜 위에 객체지향의 논리를 얹는 작업이었습니다.

---

## 2. 개인 학습 : 문법의 체화와 문제 해결 능력


오후에는 Day 8과 Day 9의 과제를 통해 파이썬의 기초부터 심화 개념, 그리고 알고리즘까지 폭넓게 복습했습니다.

### Day 8 : 데이터 조작과 객체지향 심화
기본 문법 문제에서는 리스트 슬라이싱과 딕셔너리 처리, 그리고 중첩 리스트의 평탄화(Flatten) 작업을 수행하며 데이터를 자유자재로 다루는 훈련을 했습니다. 특히 리스트 컴프리헨션을 통해 조건문을 간결하게 표현하는 법을 익혔습니다.

객체지향 심화 파트에서는 상속과 오버라이딩을 집중적으로 다루었습니다. 몬스터 클래스를 상속받은 슬라임 클래스를 구현하며 super()를 통해 부모의 초기화 로직을 재사용하고, 자신만의 속성을 추가하는 구조를 설계했습니다. 또한 플레이어가 아이템을 획득할 때 무게 제한을 두는 로직을 구현하며, 객체 간의 협력이란 단순히 데이터를 주고받는 것을 넘어 상태를 검증하고 제어하는 과정임을 확인했습니다.

알고리즘 파트에서는 문자열과 정렬을 다루었습니다. 문자가 실제로는 아스키 코드라는 숫자로 변환된다는 점을 이용해 알파벳의 위치를 찾거나, sort 함수의 key 인자에 람다(lambda)를 적용하여 길이 우선, 알파벳 차선이라는 복합 정렬 조건을 처리했습니다.

### Day 9 : 모듈 활용과 통합 구현
Day 9에서는 random, math, time과 같은 파이썬 내장 모듈을 클래스 내부 로직에 결합하는 연습을 했습니다. 로또 번호 생성기 클래스를 만들며 random.sample을 활용해 중복 없는 데이터를 추출했고, 물건을 계산하는 클래스에서는 try-except 구문을 통해 잘못된 입력값이 들어왔을 때 프로그램이 멈추지 않고 예외를 처리하도록 만들었습니다.

알고리즘 문제인 상수(백준 2908)를 풀면서는 문자열 슬라이싱([::-1])을 통해 숫자를 뒤집는 로직을 구현했습니다. 이는 데이터를 어떤 타입으로 바라보느냐에 따라 문제 해결의 난이도가 달라질 수 있음을 보여주는 사례였습니다.
```python

# [FastAPI 예시] : Pydantic 모델(Class)과 API의 결합
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

# 1. 데이터의 형태(Schema)를 클래스로 정의
# 이전에 배운 Class 문법이 데이터 검증 도구로 확장됩니다.
class Item(BaseModel):
    name: string
    price: float
    is_offer: bool = None

# 2. API 엔드포인트 구현
# HTTP GET 요청을 받아 로직을 처리하고 결과를 반환합니다.
@app.get("/items/{item_id}")
def read_item(item_id: int, q: str = None):
    # 비즈니스 로직이 들어가는 자리
    return {"item_id": item_id, "q": q}

# 3. 데이터 생성 (POST)
# 정의한 Item 클래스 형식을 따르는 데이터만 받아들입니다.
@app.post("/items/")
def create_item(item: Item):
    return {"item_name": item.name, "item_price": item.price}

# [Day 8 Algo] 람다를 활용한 복합 정렬
# 1. 길이 순서로 정렬하고, 2. 길이가 같다면 사전 순으로 정렬
data = ['apple', 'watermelon', 'kiwi', 'banana']
data.sort(key=lambda x: (len(x), x))